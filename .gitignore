# Dependências
node_modules/

# Saída de build
dist/
.astro/
.output/

# Caches de deploy / ferramentas
.wrangler/
.vercel/
.netlify/
.cache/

# Env e segredos
.env
.env.*

# Override de toolchain por máquina (o mise.toml versionado é a fonte da verdade)
mise.local.toml

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# SO / editor
.DS_Store
Thumbs.db
.idea/
*.iml

# Skills de agente externas (com lockfile de proveniência) — não são código
# deste projeto. Fora do versionamento por ora; decisão a reavaliar depois.
# `astro-expert` veio da mesma origem (registrada no skills-lock.json, importada
# de smithery.ai). As skills PRÓPRIAS do repositório — .claude/skills/astro/ e
# .claude/skills/commit/ — continuam versionadas, e é para continuarem.
.agents/
skills-lock.json
.claude/skills/astro-expert/
